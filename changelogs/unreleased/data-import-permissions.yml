---
title: Add data import permissions
type: feature
description: |
  For the upcoming advance import/export feature (enterprise only),
  we need more fine grained data import permissions support.
  
  This change adds an additional `canImport` permission option for
  objects and fields.
