---
title: Refactor domain context of collection field search request
type: change
description: |
  Restructured `_domainContext` for collection field search request:

  ```json
  {
      "_model",
      "_field",
      "_field_ids",
      "_parent": {
          "id",
          "_model"
      }
  }
  ```
