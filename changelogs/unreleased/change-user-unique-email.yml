---
title: User email now has unique constraint
type: change
description: |
  You may execute this SQL statement for migration:

  ```sql
  UPDATE auth_user SET email = lower(email);
  ALTER TABLE auth_user ADD CONSTRAINT uk_auth_user_email UNIQUE (email);
  ```
