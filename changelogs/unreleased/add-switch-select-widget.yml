---
title: Add switch select widget
type: feature
description: |
  This provides the ability to pick a choice from a multiple-choice list.
  It can be used on `selection`, `enum` and `many-to-one` fields. It supports icons and `x-labels` option for hiding labels.
