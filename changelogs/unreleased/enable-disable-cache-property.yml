---
title: Allow to enable/disable cache from `application.properties`
type: feature
description: |
  `javax.persistence.sharedCache.mode` property in `application.properties` can be used
  to overwrite the `shared-cache-mode` from `persistence.xml`. It allow to enable/disable
  the shared cache mode (ie second-level cache).
