apply plugin: "maven"

group "com.axelor"
version "3.1.0"

def rcNumber = project.hasProperty("rcNumber") ?
			   project.getProperty("rcNumber").toInteger() : null;

if (rcNumber != null) {
	version += "-rc$rcNumber"
}
